{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pnpm install && BASE_PATH=/ PORT=3000 pnpm --filter @workspace/kid-store run build && pnpm --filter @workspace/api-server run build"
  },
  "deploy": {
    "startCommand": "NODE_ENV=production node --enable-source-maps backend/dist/index.mjs",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
