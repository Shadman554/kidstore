[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm install -g pnpm@10 && pnpm install --frozen-lockfile"]

[start]
cmd = "NODE_ENV=production node --enable-source-maps backend/dist/index.mjs"
